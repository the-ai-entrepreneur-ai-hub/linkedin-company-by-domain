{
    "actorOutputSchemaVersion": 1,
    "title": "Resolved LinkedIn companies",
    "properties": {
        "companies": {
            "type": "string",
            "title": "Resolved companies (all rows)",
            "description": "One row per domain: verified confidence (high/medium/low), resolution method, company name, LinkedIn URL, employee count and followers. Unresolved domains emit free rows carrying a qualityState explaining why. Monitor mode adds free diff-summary rows.",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
